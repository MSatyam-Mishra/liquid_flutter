name: liquid_flutter_example
description: Notes workspace example for liquid_flutter.
publish_to: none

environment:
  sdk: ^3.10.9

dependencies:
  flutter:
    sdk: flutter
  liquid_flutter:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
